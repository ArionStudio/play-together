import { PlaceholderPage } from "@/features/shell/placeholder-page.tsx"

export function PartyPage() {
  return (
    <PlaceholderPage
      title="Party flow is not live yet"
      description="Parties stay separate from lobbies so the same group can move between invites, matchmaking, and rematches later without changing the social model."
    />
  )
}
